node_modules/
.env
.env.*
!.env.example

# Hardhat
artifacts/
cache/
typechain-types/
coverage/
coverage.json
dist/

# Deployment address books are intentionally COMMITTED — they are submission
# material and let anyone reproduce the BscScan links.
!deployments/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Frontend
app/node_modules/
app/dist/
# app/src/lib/generated.js is generated by scripts/export-abi.ts, but it is
# COMMITTED on purpose: it is the frontend's deployment manifest (public ABIs and
# public addresses), and the Vercel build has no Hardhat artifacts to derive it
# from. Regenerate with `npm run abi` after every deploy.

# Pitch deck — a business document, not source. Kept out of the public repo.
DEXless Deck_RWA.pdf
*.pdf
!export/*.pdf
